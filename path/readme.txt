(2013.06.08)

PHPを実行した場所がカレントディレクトリ。
require や include のパスで相対指定する場合、
カレントディレクトリをベースにファイルが読み込まれる。

dirname(__FILE__) を使って読みこめば、
カレントディレクトリに依存しない読み込みが可能。

【例】
require( dirname(__FILE__)."/../target.php" );


